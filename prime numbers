#include<stdio.h>
int main()
{
	int n,i;
	printf("Enter n numbers\n");
	scanf("%d",&n);
	if(n%i==0&&n%2==1)
	{
		printf("%d is a prime number",n);
	}
			else
		{
		printf("%d is not a prime number",n);
	}
}
